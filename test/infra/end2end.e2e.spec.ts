import { describe, expect, it } from 'vitest';

describe('E2E TEST', () => {
  it('should return true', () => {
    const actual = true;
    const expected = true;

    expect(actual).toBe(expected);
  });
});
